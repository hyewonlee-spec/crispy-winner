# Phase 10.3 Fixed Build

This fixes a Vercel build error caused by a misplaced JSX brace in the Today page Cycle-aware training card.

Also adjusted:
- Check tab cycle card is hidden when menstruation tracking is disabled.
- Bottom navigation class now uses the correct 5/6 button layout based on active workout + cycle tracking.
